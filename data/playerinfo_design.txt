プレイヤー情報の設計：
持つべき情報

プレイヤー名 -> string
ユーザid -> int
現在地 -> int
コマの色 -> string or int
状態(ゴール、未ゴール)　-> boolean or int
状態異常(〇回休みなど) -> int
順位 -> int (別のモデルに持たせてもいい)
順番 -> int (これも別にユーザ自身が持たなくてもいい？　むしろ配列などで識別番号をシャッフルして管理すべきかも)